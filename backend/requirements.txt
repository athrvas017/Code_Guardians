--extra-index-url https://download.pytorch.org/whl/cpu

# Core Flask dependencies
Flask==3.0.3
gunicorn
requests==2.32.3
validators==0.22.0
python-dotenv==1.0.1

# ML/Data Processing
pandas>=2.0.0
# Pinning scikit-learn to avoid version mismatch with pre-trained models
# If you trained locally with a specific version, update this to match closer (e.g. ==1.3.2)
scikit-learn==1.3.2
joblib>=1.3.0

# AI Image Detection (Real vs AI-Generated) - CPU Only versions via extra-index-url
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
datasets>=2.14.0
Pillow>=10.0.0
